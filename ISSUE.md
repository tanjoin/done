## Google カレンダーと連携したい
- TODO となるカレンダー
- DONE カレンダー（既にある）
### 認証
- OAuth 2.0 Client ID は暗号化して localStorage に保存する
- 処理するカレンダーはAPIで一覧を取得し select で選択する（選択した情報は localStorage に暗号化して保存する）
- localStorage の key は他のプロジェクトと被らないようにする
- 設定は「設定・データ管理」の画面で行う
- README.md に設定方法を記載する
### 内容
- TODO カレンダーの予定を「表示設定」をもとに取得する（翌日のタスクまで）
  - 取得する件数はそこそこ多くする
  - 完了が押されたらグラファイト・キャンセルが押されたらフラミンゴの色にする
- 完了ボタンは今まで通り
- 追加ボタンの場合に DONE カレンダーに予定を追加する
- 新たに「追記」ボタンのステータスを作る
  - これは今まで通り、URL 経由で Google カレンダーの予定作成画面を開く
- 完了・追加・追記のボタンの色を変える。青・緑・黄のようにする。
- ステータスについては日付情報が存在するので今まで通り行う
- カレンダーのタスクであることを示すためにグループは「カレンダー」とする
- TODOカレンダーのタスクは基本的に「完了」ボタンで、TODOカレンダーのタスクの色を変えることで処理する
  - 完了済みのタスクはグラファイト色にする
  - キャンセル済みのタスクはフラミンゴ色にする
- 追記・追加ボタンはDONEカレンダー専用機能となる
  - 処理のさせ方は前述通り
- 完了は `skipCalendarOnComplete` が true の場合または TODO カレンダーのタスクの場合に行う
- 追加は `skipCalendarOnComplete` が false の場合かつ DONE カレンダーのタスクの場合に行う
- 追記は `createTaskViaUrl` が true の場合かつ DONE カレンダーのタスクの場合に行う

## Google ドライブと連携したい
- ON / OFF する設定を「設定・データ管理」の画面で行う
- JSON データを Google ドライブに保存する
  - 他の Google ドライブのデータに影響がないようにする
- 認証については上記と同じ方法をとる
  - README.md に設定方法を記載する
  - 各種設定は暗号化して localStorage に保存する
### 内容
- JSON データを Google ドライブに保存する
- 各種タスクのボタンが押された後に JSON データを Google ドライブに保存する
- 既にログイン済みであれば、開いたときに自動で読み込む
- 既存のインポート・エクスポートはそのまま。ただし、インポートされたときは同期するために Google ドライブに保存する
